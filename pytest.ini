[pytest]
testpaths = tests
python_files = test_*.py
; Without this, `from app import create_app` fails with
; ModuleNotFoundError in a clean CI checkout -- there's no setup.py/
; pyproject packaging step installing this project, so pytest has to be
; told explicitly to put the repo root on sys.path itself.
pythonpath = .
